[package]
org = "claimflow"
name = "claims"
version = "0.1.0"
distribution = "@DISTRIBUTION@"

# The workflow module: the prebuilt 0.10 task-model build, pushed to the builder's local repository.
@WORKFLOW_DEP@

[build-options]
observabilityIncluded = true
