[package]
org = "claimflow"
name = "notifications"
version = "0.1.0"
distribution = "2201.13.4"

# No dependency blocks: the ICP runtime bridge resolves from Ballerina Central, and this
# integration runs no workflows.
[build-options]
observabilityIncluded = true
