[package]
org = "claimflow"
name = "claims_agent"
version = "0.1.0"
distribution = "2201.13.4"

# The workflow module: released by default, or the prebuilt observability build
# when build.sh runs with --with-observability.
@WORKFLOW_DEP@

[build-options]
observabilityIncluded = true
